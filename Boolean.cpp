#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
int main()
{
    bool syproseIsFit = false;
	
	printf("is syprose fit (1 is yes 0 is no)? %i\n", syproseIsFit);
	
	printf("%d\n", syproseIsFit + 10);
	
	return 0;
}
	/*boolean data is anything that is either true or false. 1= true while 0=false
	to de declaire these variables, we useunderscore Bool followed by name of variable. 
	thename of variable commences with a lowercase for first word followed by capslock for next words or separated with underscore*/ 